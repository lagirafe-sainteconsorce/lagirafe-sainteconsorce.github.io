// Example file src/helpers/event.js
module.exports = function(options) {
    // tentative de passer une var avec la date now
    const start = Date.now();
    return start;
    console.log(start);
}
